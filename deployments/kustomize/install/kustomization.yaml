apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: xjelinekj-ambulance-webapi-mongodb-init
    files:
      - params/init-db.js
  - name: xjelinekj-ambulance-webapi-config
    literals:
      - database=xjelinekj-ambulance
      - collection=ambulance
patches:
  - path: patches/webapi.deployment.yaml
    target:
      group: apps
      version: v1
      kind: Deployment
      name: xjelinekj-ambulance-webapi

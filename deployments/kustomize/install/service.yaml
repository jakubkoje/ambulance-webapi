kind: Service
apiVersion: v1
metadata:
  name: xjelinekj-ambulance-webapi
spec:
  selector:
    pod: xjelinekj-ambulance-webapi-label
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: webapi-port
